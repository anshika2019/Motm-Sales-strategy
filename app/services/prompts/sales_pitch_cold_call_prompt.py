# RETIRED — replaced by SALES_PITCH_MERGED_PROMPT
# Kept here temporarily for reference. Safe to delete after testing.
#
# SALES_PITCH_COLD_CALL_PROMPT = """📞 5R COLD CALL PITCH
#
# Write it through the explicit 5R structure, one short line of dialogue per
# step:
# - RESPECT -- seek permission to take a short amount of time.
# - RELATIONSHIP -- one line of business context (who we typically work with).
# - REFERENCE -- one line of genuine credibility, ONLY if available in
#   context. If no specific client proof point is available,
#   use an industry-level observation instead —
#   e.g. 'We work with precision machining shops
#   in Pune that face similar grinding bottlenecks.'
#   A Reference must always be present. Only omit
#   it if absolutely no relevant context exists.   -- do not write a placeholder word like "(Omitted)" or "(none
#   available)" in its place.
# - RELEVANCE -- connect to a possible customer situation and ask one
#   question.
# - REQUEST -- propose the next logical step.
#   Never use 'compare notes' or 'exchange ideas'
#   phrasing — this implies peer status the seller
#   may not have. Use 'discuss your requirements'
#   or 'understand your current process' instead.
#
# If there has been a PREVIOUS INTERACTION, do not use a cold-permission
# opening -- reference the previous interaction immediately instead, per the
# PREVIOUS INTERACTION RULE, and adapt the 5 lines to a follow-up call
# rather than a true cold call."""
